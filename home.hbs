{{!< default}}

<div class="w-full h-screen bg-cover" style="background-image: url('{{#if @custom.use_custom_cover}}{{img_url @site.cover_image}}{{else}}{{asset 'img/background-1.jpeg'}}{{/if}}')">
    <div class="container mx-auto">
        <h1 class="text-6xl font-bold text-center pt-16 text-white">{{@site.title}}</h1>
        <p class="text-white text-2xl text-center mt-4">{{@site.description}}</p>
        <div class="flex justify-center mt-16">
            <div class="bg-cover border-8 border-rose-400 rounded-full w-52 h-52 overflow-hidden" style="background-image: url('{{#if @custom.avatar_image}}{{img_url @custom.avatar_image}}{{else}}{{asset 'img/avatar.jpeg'}}{{/if}}'); background-radius: 50% 50% 50%; background-position: 50% 50%;">
            </div>
        </div>
        <div class="my-12 w-full text-center">
            {{#if @site.facebook}}
            <a href="{{@site.facebook}}" target="_blank" rel="noopener nofollow">
                <i class="text-white text-3xl leading-lg fa-brands fa-facebook-f"></i>
            </a>
            {{/if}}
            {{#if @site.twitter}}
            <a class="ml-6" href="{{@site.twitter}}" target="_blank" rel="noopener nonofollow">
                <i class="text-white text-3xl leading-lg fa-brands fa-twitter"></i>
            </a>
            {{/if}}
            {{#if @custom.instagram}}
            <a class="ml-6" href="{{@custom.instagram}}" target="_blank" rel="noopener nofollow">
                <i class="text-white text-3xl leading-lg fa-brands fa-instagram"></i>
            </a>
            {{/if}}
            {{#if @custom.linkedin}}
            <a class="ml-6" href="{{@custom.linkedin}}" target="_blank" rel="noopener nofollow">
                <i class="text-white text-3xl leading-lg fa-brands fa-linkedin-in"></i>
            </a>
            {{/if}}
            {{#if @custom.email}}
            <a class="ml-6" href="mailto:{{@custom.email}}" target="_blank" rel="noopener nonofollow">
                <i class="text-white text-3xl leading-lg fa-solid fa-envelope"></i>
            </a>
            {{/if}}
        </div>
    </div>
</div>
{{#post}}
<div class="container mx-auto px-10 lg:px-52">
    <div class="my-12 prose prose-slate dark:prose-dark max-w-full">
        {{content}}
    </div>
</div>
{{/post}}